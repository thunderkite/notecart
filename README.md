# NoteCart — Notebook + Shop Platform

**Веб-приложение блокнот** с интегрированным магазином, поддержкой ролей, поиском и обратной связью.

## 🚀 Функционал

### ✅ Реализованные требования

1. **Личный кабинет** — профиль, смена пароля, контактной информации, предпочтений
2. **База данных** — SQLite с ORM (SQLAlchemy), таблицы для пользователей, заметок, товаров, заказов
3. **Регистрация/Авторизация** — email + пароль, сессии (Flask-Login)
4. **Роли** — Администратор (доступ к админ-панели) и Пользователь
5. **Удобный функционал** — CRUD заметок, управление корзиной, оформление заказов
6. **Поиск** — глобальный поиск по заметкам и товарам через `/api/search`
7. **Обратная связь** — форма отзывов с оценкой (анонимно или от пользователя)
8. **Магазин** — каталог товаров, фильтры, корзина в сессии, оформление заказа с уменьшением склада

---

## 📦 Установка и запуск

### Требования
- Python 3.10+
- pip

### Быстрый старт

```powershell
# Клонируйте/скачайте проект
cd book

# Создайте виртуальное окружение
python -m venv .venv
.venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

# Инициализируйте базу данных и демо-данные
$env:FLASK_APP="run"
flask init-db
flask seed-db

# Запустите сервер разработки
flask run
```

Откройте браузер: [http://127.0.0.1:5000](http://127.0.0.1:5000)

---

## 👥 Тестовые аккаунты

После выполнения `flask seed-db`:

| Email                | Пароль     | Роль          |
|---------------------|-----------|---------------|
| admin@example.com   | Admin123! | Администратор |
| user@example.com    | User123!  | Пользователь  |

---

## 🗂️ Структура проекта

```
book/
├── app/
│   ├── __init__.py       # Фабрика приложения
│   ├── models.py         # Модели БД (User, Note, Product, Order...)
│   ├── api.py            # REST API эндпойнты
│   ├── views.py          # Роуты для HTML-страниц
│   ├── utils.py          # Хелперы, декораторы ролей
│   └── cli.py            # Команды CLI (init-db, seed-db)
├── templates/            # HTML шаблоны
│   ├── base.html
│   ├── index.html        # Лендинг + логин/регистрация
│   ├── dashboard.html    # Управление заметками
│   ├── shop.html         # Магазин + корзина
│   └── admin.html        # Админ-панель
├── static/
│   ├── css/main.css      # Стили
│   └── js/
│       ├── api.js        # Обёртка fetch для API
│       ├── dashboard.js  # Логика заметок
│       └── shop.js       # Логика магазина
├── instance/             # База данных (app.db)
├── tests/                # Автотесты
├── requirements.txt
├── run.py                # Точка входа
└── README.md
```

---

## 🔗 API Endpoints

### Аутентификация
- `POST /api/auth/register` — регистрация (email, password, name, phone)
- `POST /api/auth/login` — вход
- `POST /api/auth/logout` — выход
- `GET /api/auth/me` — текущий пользователь
- `PUT /api/auth/profile` — обновление профиля
- `PUT /api/auth/password` — смена пароля
- `PUT /api/auth/preferences` — сохранение предпочтений (JSON)

### Заметки
- `GET /api/notes?q=...` — список заметок (с поиском)
- `POST /api/notes` — создать заметку
- `PUT /api/notes/:id` — обновить
- `DELETE /api/notes/:id` — удалить

### Поиск
- `GET /api/search?q=...` — глобальный поиск по заметкам и товарам

### Обратная связь
- `POST /api/feedback` — отправить отзыв (message, rating)
- `GET /api/feedback` — список отзывов (только админ)

### Магазин
- `GET /api/products?q=...&category=...&max_price=...` — список товаров с фильтрами
- `GET /api/cart` — содержимое корзины
- `POST /api/cart` — добавить товар (product_id, quantity)
- `DELETE /api/cart/:product_id` — удалить товар
- `POST /api/cart/clear` — очистить корзину
- `POST /api/checkout` — оформить заказ (создаёт Order, уменьшает склад)

### Админ
- `GET /api/admin/users` — список пользователей
- `GET /api/admin/orders` — список заказов

---

## 🧪 Тестирование

```powershell
# Запуск автотестов
pytest
```

Тесты покрывают:
- Регистрацию/логин
- CRUD заметок
- Корзину и оформление заказа
- Доступ по ролям

---

## 🛠️ Технологии

- **Backend**: Python 3.10, Flask 3.0, SQLAlchemy, Flask-Login, Flask-Bcrypt
- **Frontend**: Vanilla HTML/CSS/JS (без фреймворков)
- **Database**: SQLite (легковесная файловая БД)
- **Testing**: pytest, Flask test client

---

## 📝 Лицензия

MIT — свободное использование.

---

## 🤝 Контакты

По вопросам и предложениям создавайте Issue в репозитории проекта.

# 📝 NoteCart — Элегантный блокнот в стиле Apple

**Минималистичное веб-приложение для заметок** с красивым дизайном, вдохновлённым Apple Notes. Чистый интерфейс, плавные анимации, удобное управление заметками.

## ✨ Особенности

- 🎨 **Дизайн премиум-класса** — стиль Apple с плавными анимациями и современным UI
- 📱 **Адаптивная сетка карточек** — заметки отображаются красивыми карточками с превью
- ⚡ **Модальный редактор** — полноэкранное окно для комфортного написания с плавными переходами
- � **Мгновенный поиск** — найдите нужную заметку за секунду
- 🔐 **Безопасность** — регистрация, авторизация, защищённые сессии
- 👥 **Роли пользователей** — обычные пользователи и администраторы
- 💬 **Обратная связь** — встроенная система отзывов

## 🚀 Функционал

1. **Личный кабинет** — управление профилем, смена пароля, персональные настройки
2. **База данных** — SQLite с ORM (SQLAlchemy) для хранения заметок и пользователей
3. **Регистрация/Авторизация** — безопасная система входа с хешированием паролей
4. **Роли** — Администратор (доступ к панели управления) и Пользователь
5. **Управление заметками** — создание, редактирование, удаление с красивым интерфейсом
6. **Поиск** — быстрый поиск по заголовкам, содержимому и тегам
7. **Обратная связь** — отправка отзывов и предложений
8. **Теги** — организация заметок с помощью меток

---

## 📦 Установка и запуск

### Требования
- Python 3.10+
- pip

### Быстрый старт

```powershell
# Клонируйте/скачайте проект
cd book

# Создайте виртуальное окружение
python -m venv .venv
.venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

# Инициализируйте базу данных и демо-данные
$env:FLASK_APP="run"
flask init-db
flask seed-db

# Запустите сервер разработки
flask run
```

Откройте браузер: [http://127.0.0.1:5000](http://127.0.0.1:5000)

---

## 👥 Тестовые аккаунты

После выполнения `flask seed-db`:

| Email                | Пароль     | Роль          |
|---------------------|-----------|---------------|
| admin@example.com   | Admin123! | Администратор |
| user@example.com    | User123!  | Пользователь  |

---

## 🗂️ Структура проекта

```
notecart/
├── app/
│   ├── __init__.py       # Фабрика Flask-приложения
│   ├── models.py         # SQLAlchemy модели (User, Note, Feedback)
│   ├── api.py            # REST API endpoints
│   ├── views.py          # HTML-страницы (/, /dashboard, /admin)
│   ├── utils.py          # Хелперы, декораторы ролей, сериализаторы
│   └── cli.py            # CLI-команды (init-db, seed-db)
├── templates/            # Jinja2 шаблоны
│   ├── base.html         # Базовый layout
│   ├── index.html        # Страница входа/регистрации
│   ├── dashboard.html    # Интерфейс управления заметками
│   └── admin.html        # Панель администратора
├── static/
│   ├── css/
│   │   └── main.css      # Стили в стиле Apple
│   └── js/
│       ├── api.js        # Fetch обёртка + утилиты
│       └── dashboard.js  # Логика работы с заметками
├── instance/             # База данных SQLite (app.db)
├── tests/                # Автотесты (pytest)
│   ├── __init__.py
│   └── test_api.py       # Тесты API endpoints
├── .gitignore
├── requirements.txt      # Python зависимости
├── run.py                # Точка входа приложения
└── README.md
```

---

## 🔗 API Endpoints

### 🔐 Аутентификация
- `POST /api/auth/register` — регистрация нового пользователя
- `POST /api/auth/login` — вход в систему
- `POST /api/auth/logout` — выход
- `GET /api/auth/me` — информация о текущем пользователе
- `PUT /api/auth/profile` — обновление профиля (имя, телефон)
- `PUT /api/auth/password` — смена пароля
- `PUT /api/auth/preferences` — сохранение пользовательских настроек

### 📝 Заметки
- `GET /api/notes` — получить все заметки пользователя
- `GET /api/notes?q=текст` — поиск по заметкам
- `POST /api/notes` — создать новую заметку
- `PUT /api/notes/:id` — обновить заметку
- `DELETE /api/notes/:id` — удалить заметку

### 🔍 Поиск
- `GET /api/search?q=текст` — глобальный поиск по заметкам

### 💬 Обратная связь
- `POST /api/feedback` — отправить отзыв
- `GET /api/feedback` — просмотр всех отзывов (только для администратора)

### 👨‍💼 Администрирование
- `GET /api/admin/users` — список всех пользователей
- `GET /api/admin/orders` — история операций

---

## 💡 Использование

### Работа с заметками

1. **Создание заметки**
   - Нажмите кнопку "Новая заметка" с иконкой +
   - Откроется модальное окно с плавной анимацией
   - Введите заголовок, текст и теги (опционально)
   - Нажмите "Сохранить"

2. **Редактирование**
   - Нажмите "Редактировать" под карточкой заметки
   - Измените содержимое в модальном окне
   - Сохраните изменения

3. **Поиск**
   - Начните вводить в строке поиска
   - Результаты обновятся мгновенно
   - Поиск работает по заголовку, тексту и тегам

4. **Удаление**
   - Нажмите "Удалить" под заметкой
   - Подтвердите действие

---

## 🧪 Тестирование

```powershell
# Запуск всех тестов
python -m pytest tests/test_api.py -v

# Быстрая проверка
pytest
```

Автотесты покрывают:
- ✅ Регистрацию и авторизацию пользователей
- ✅ CRUD операции с заметками
- ✅ Поиск по заметкам
- ✅ Разграничение доступа по ролям
- ✅ API endpoints для администратора

---

## 🛠️ Технологии

### Backend
- **Python 3.10+** — современный язык программирования
- **Flask 3.0** — лёгкий веб-фреймворк
- **SQLAlchemy** — ORM для работы с базой данных
- **Flask-Login** — управление сессиями пользователей
- **Flask-Bcrypt** — безопасное хеширование паролей

### Frontend
- **Vanilla HTML/CSS/JS** — без фреймворков, чистый код
- **Apple-inspired Design** — минималистичный UI с плавными анимациями
- **CSS Grid & Flexbox** — современная адаптивная вёрстка
- **Fetch API** — асинхронные запросы к серверу

### Database
- **SQLite** — легковесная файловая база данных
- Не требует отдельного сервера
- Идеально для небольших проектов

### Testing
- **pytest** — фреймворк для тестирования
- **Flask test client** — тестирование API endpoints
- 8 автотестов с покрытием основного функционала

---

## 🎨 Особенности дизайна

Интерфейс вдохновлён приложениями Apple и следует принципам их дизайн-системы:

- **Минимализм** — никаких лишних элементов, фокус на контенте
- **Плавные анимации** — все переходы и появления элементов анимированы
- **Чистая типографика** — системный шрифт SF Pro Display
- **Воздух в интерфейсе** — достаточно свободного пространства
- **Карточный дизайн** — заметки как отдельные карточки с тенями
- **Модальные окна** — редактор заметок открывается поверх с эффектом blur
- **Hover-эффекты** — интерактивная реакция на действия пользователя
- **Цветовая палитра**:
  - Фон: светло-серый (#f5f5f7)
  - Акцент: синий Apple (#007aff)
  - Текст: чёрный (#1d1d1f) и серый (#86868b)

---

## 📝 Лицензия

MIT — свободное использование.

---

## 🤝 Контакты

По вопросам и предложениям создавайте Issue в репозитории проекта.

---

## 📸 Скриншоты

### Главная страница
Чистая страница входа с двумя формами — вход и регистрация.

### Дашборд заметок
Сетка карточек с заметками, строка поиска и кнопка создания новой заметки.

### Модальный редактор
Полноэкранное окно для создания и редактирования заметок с размытым фоном.

---

**Сделано с ❤️ в стиле Apple**
